#include<stdio.h>
#include<stdlib.h>
int main()
{
    int val; //valore
    printf("inserisci un valore ");
    scanf("%d", &val);
    if (val > 3)
        printf("il valore e\' > 3");
    else
            printf("il valore non e\' > 3");
}

